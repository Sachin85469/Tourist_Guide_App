<?xml version="1.0" encoding="utf-8"?>
<androidx.coordinatorlayout.widget.CoordinatorLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/bg_light">

    <com.google.android.material.appbar.AppBarLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:background="?attr/colorSurface"
        app:elevation="0dp">

        <com.google.android.material.appbar.MaterialToolbar
            android:id="@+id/toolbar"
            android:layout_width="match_parent"
            android:layout_height="?attr/actionBarSize"
            app:navigationIcon="@drawable/ic_back"
            app:navigationIconTint="?attr/colorOnSurface"
            app:titleTextAppearance="@style/TextAppearance.MaterialComponents.Headline6"
            app:title="@string/profile" />
    </com.google.android.material.appbar.AppBarLayout>

    <androidx.core.widget.NestedScrollView
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        app:layout_behavior="@string/appbar_scrolling_view_behavior">

        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="vertical"
            android:paddingBottom="88dp">

            <!-- Premium Profile Header -->
            <include
                android:id="@+id/profileHeader"
                layout="@layout/component_profile_header" />

            <!-- Premium Profile Completion Card -->
            <include
                android:id="@+id/completionCard"
                layout="@layout/component_completion_card" />

            <!-- Premium Stats Section -->
            <include
                android:id="@+id/statsSection"
                layout="@layout/component_stats_section" />


            <!-- Premium Recent Activity -->
            <include
                android:id="@+id/recentActivitySection"
                layout="@layout/component_recent_activity" />

            <!-- Premium Quick Actions -->
            <include
                android:id="@+id/quickActionsSection"
                layout="@layout/component_quick_actions" />

            <!-- Premium Settings Section -->
            <include
                android:id="@+id/settingsSection"
                layout="@layout/component_settings_section" />

        </LinearLayout>
    </androidx.core.widget.NestedScrollView>

</androidx.coordinatorlayout.widget.CoordinatorLayout>
