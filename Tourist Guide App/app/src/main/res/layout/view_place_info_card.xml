<?xml version="1.0" encoding="utf-8"?>
<com.google.android.material.card.MaterialCardView xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:layout_margin="5dp"
    android:stateListAnimator="@animator/card_lift"
    app:cardBackgroundColor="@color/color_surface"
    app:cardCornerRadius="@dimen/premium_card_radius"
    app:cardElevation="3dp"
    app:strokeColor="@color/premium_card_stroke"
    app:strokeWidth="1dp">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:minHeight="136dp"
        android:padding="16dp">

        <FrameLayout
            android:layout_width="44dp"
            android:layout_height="44dp"
            android:background="@drawable/bg_lavender_icon_circle">

            <ImageView
                android:id="@+id/ivInfoIcon"
                android:layout_width="24dp"
                android:layout_height="24dp"
                android:layout_gravity="center"
                android:contentDescription="@null"
                app:tint="@color/color_primary_dark"
                tools:src="@drawable/ic_ms_info" />
        </FrameLayout>

        <TextView
            android:id="@+id/tvInfoTitle"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginTop="12dp"
            android:ellipsize="end"
            android:maxLines="1"
            android:textAppearance="@style/TextAppearance.App.PlaceCardTitle"
            tools:text="Best Time" />

        <TextView
            android:id="@+id/tvInfoValue"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginTop="5dp"
            android:ellipsize="end"
            android:lineSpacingExtra="2dp"
            android:maxLines="2"
            android:textAppearance="@style/TextAppearance.App.PlaceBody"
            tools:text="Oct-Mar" />
    </LinearLayout>
</com.google.android.material.card.MaterialCardView>
